apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-stable
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: nginx-canary.labaq.life
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: nginx-canary
            port:
              number: 80
