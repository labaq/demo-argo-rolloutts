apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default

namePrefix:
nameSuffix:

resources:
  - deploy.yaml
  - cm.yaml
  - svc-stable.yaml
  - svc-canary.yaml
  - gateway.yaml
  - virtualservice.yaml
  - rollout.yaml

